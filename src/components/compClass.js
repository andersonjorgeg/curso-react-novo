import { Component } from 'react'; // Importa a classe `Component` da biblioteca `react`

class App2 extends Component { // Define uma nova classe chamada `App2` que estende a classe `Component`
  render() { // Define o método `render()` para a classe `App2`
    return ( // Retorna um elemento JSX para ser renderizado na página
      <h1>Ola mundo</h1> // Um elemento `<h1>` com o texto "Ola mundo"
    )
  }
}

export default App2; // Exporta a classe `App2` como a exportação padrão
